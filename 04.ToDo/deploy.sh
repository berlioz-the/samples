berlioz-cli login --user ruben --pass Kukareku1234!

# berlioz-cli build
berlioz-cli push

# berlioz-cli  process --deployment prod --region us-east-1 --cluster todo --stage single-stage-deploy
# berlioz-cli  process --deployment prod --region us-east-1 --cluster todo --stage extract-current,construct-desired,output-delta,process-delta

# berlioz-cli  process --deployment prod --region us-east-1 --cluster anlytc --stage single-stage-deploy
# berlioz-cli  process --deployment prod --region us-east-1 --cluster anlytc --stage extract-current,construct-desired,output-delta,process-delta 
